<!--<Page x:Class="WpfTali.Pages.WorkoutsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="WorkoutsPage" Background="#FFE4E1" FlowDirection="RightToLeft">

    <Grid>
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0,0,0,15">
                <StackPanel HorizontalAlignment="Right">
                    <TextBlock x:Name="MonthYearTextBlock" Text="חודש יוני 2026" FontSize="26" FontWeight="Bold" Foreground="#FF1493"/>
                    <TextBlock Text="לוח אימונים חודשי וניהול הרשמות" FontSize="14" Foreground="#666"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center">
                    <Button x:Name="BtnBackToHome" Content="🏠 חזרה לבית" Margin="5" Padding="10,6" 
                            Background="White" Foreground="#FF1493" BorderBrush="#FF1493" BorderThickness="1.5"
                            FontWeight="Bold" Cursor="Hand" Click="BtnBackToHome_Click">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="6"/>
                            </Style>
                        </Button.Resources>
                    </Button>

                    <Button x:Name="PrevMonthButton" Content="◀ החודש הקודם" Margin="5" Padding="8,4"  Background="White" Foreground="#FF1493" BorderBrush="#FF1493" BorderThickness="1.5" FontWeight="Bold" Cursor="Hand" Click="PrevMonthButton_Click"/>
                    <Button x:Name="NextMonthButton" Content="החודש הבא ▶" Margin="5" Padding="8,4" Background="White" Foreground="#FF1493" BorderBrush="#FF1493" BorderThickness="1.5" FontWeight="Bold" Cursor="Hand" Click="NextMonthButton_Click"/>

                    <Button x:Name="AddWorkoutButton" Content="+" FontSize="24" FontWeight="Bold" 
                            Background="#FF1493" Foreground="White" Width="45" Height="45" Margin="15,0,0,0"
                            HorizontalAlignment="Left" VerticalAlignment="Center" Cursor="Hand" 
                            Click="AddWorkoutButton_Click" Visibility="Collapsed">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="22"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                </StackPanel>
            </Grid>

            <ListBox x:Name="DaysListBox" Grid.Row="1" Height="70" Margin="0,0,0,15" SelectionChanged="DaysListBox_SelectionChanged">
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="#FF1493" BorderThickness="1" CornerRadius="5" Padding="10,5" Margin="2" Width="60" Background="White">
                            <StackPanel HorizontalAlignment="Center">
                                <TextBlock Text="{Binding DayNumber}" FontSize="16" FontWeight="Bold" HorizontalAlignment="Center" Foreground="#FF1493"/>
                                <TextBlock Text="{Binding DayOfWeekHebrew}" FontSize="11" HorizontalAlignment="Center" Foreground="#555"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="1*"/>
                </Grid.ColumnDefinitions>

                <ListView x:Name="WorkoutsListView" Grid.Column="0" Margin="0,0,10,0" SelectionChanged="WorkoutsListView_SelectionChanged">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Border CornerRadius="8" Padding="10" Background="White" Margin="2" Width="300">
                                <StackPanel>
                                    <TextBlock Text="{Binding WorkoutName}" FontSize="16" FontWeight="Bold" Foreground="#FF1493"/>
                                    <TextBlock Text="{Binding TimeStr}" FontSize="14" FontWeight="SemiBold"/>
                                    <TextBlock Text="{Binding TrainerName, StringFormat='מאמן: \{0\}'}" FontSize="12" Foreground="#666"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

                <Border x:Name="DetailsPanel" Grid.Column="1" Background="White" CornerRadius="10" Padding="15" Visibility="Collapsed">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <StackPanel Grid.Row="0">
                            <TextBlock x:Name="DetailWorkoutName" FontSize="18" FontWeight="Bold" Foreground="#FF1493" Margin="0,0,0,10"/>
                            <TextBlock x:Name="DetailTrainer" FontSize="13" Margin="0,2"/>
                            <TextBlock x:Name="DetailDate" FontSize="13" Margin="0,2"/>
                            <TextBlock x:Name="DetailTime" FontSize="13" Margin="0,2"/>
                            <TextBlock x:Name="DetailStatus" FontSize="13" FontWeight="SemiBold" Margin="0,2,0,10"/>

                            <TextBlock Text="מתאמנים רשומים בשיעור:" FontSize="14" FontWeight="Bold" Margin="0,10,0,5"/>
                        </StackPanel>

                        <ListView x:Name="RegisteredTraineesListView" Grid.Row="1" Margin="0,0,0,10">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding First_name}" FontSize="13" Padding="5"/>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>

                        <StackPanel Grid.Row="2">
                            <Button x:Name="ActionButton" Content="הרשמה לאימון" Background="#FF1493" Foreground="White" Height="35" FontWeight="Bold" Margin="0,5" Click="ActionButton_Click"/>
                            <Button x:Name="DeleteWorkoutButton" Content="מחק אימון" Background="Red" Foreground="White" Height="35" FontWeight="Bold" Margin="0,5" Click="DeleteWorkoutButton_Click" Visibility="Collapsed"/>
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>
        </Grid>

        <Popup x:Name="AddWorkoutPopup" Placement="Center" Width="360" Height="420" StaysOpen="False">
            <Border Background="White" CornerRadius="12" Padding="20" BorderBrush="#FF1493" BorderThickness="2">
                <StackPanel>
                    <Grid Margin="0,0,0,15">
                        <TextBlock Text="הוספת אימון חדש" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center" Foreground="#FF1493"/>
                        <Button Content="✕" Background="Transparent" BorderThickness="0" Foreground="#999" FontSize="16" HorizontalAlignment="Left" Click="BtnCancelPopup_Click" Cursor="Hand"/>
                    </Grid>

                    <TextBlock Text="תאריך אימון:" Margin="0,5,0,2"/>
                    <DatePicker x:Name="DpDate" Height="30"/>

                    <TextBlock Text="שעה (לדוגמה 15:30):" Margin="0,8,0,2"/>
                    <TextBox x:Name="TxtTime" Height="30" Padding="5,2"/>

                    <TextBlock Text="קוד סוג אימון (ID):" Margin="0,8,0,2"/>
                    <TextBox x:Name="TxtKindId" Height="30" Padding="5,2"/>

                    <TextBlock Text="קוד מאמן (ID):" Margin="0,8,0,2"/>
                    <TextBox x:Name="TxtTrainerId" Height="30" Padding="5,2" Background="#EAEAEA" IsReadOnly="True" Focusable="False"/>

                    <Grid Margin="0,20,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Button Grid.Column="0" Content="שמור אימון" Click="BtnSaveWorkout_Click" Background="#FF1493" Foreground="White" FontWeight="Bold" Height="35" Margin="5" Cursor="Hand"/>
                        <Button Grid.Column="1" Content="ביטול" Click="BtnCancelPopup_Click" Background="#666" Foreground="White" FontWeight="Bold" Height="35" Margin="5" Cursor="Hand"/>
                    </Grid>
                </StackPanel>
            </Border>
        </Popup>

        <Grid x:Name="LoadingOverlay" Background="#80FFFFFF" Visibility="Visible">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="80" Height="10" Foreground="#FF1493" Background="LightGray"/>
                <TextBlock Text="טוען נתונים מהשרת, אנא המתן..." FontSize="16" FontWeight="Bold" Foreground="#FF1493" Margin="0,10,0,0" HorizontalAlignment="Center"/>
            </StackPanel>
        </Grid>
    </Grid>
</Page>-->
<Page x:Class="WpfTali.Pages.WorkoutsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="WorkoutsPage" Background="#FFE4E1" FlowDirection="RightToLeft">

    <Grid>
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0,0,0,15">
                <StackPanel HorizontalAlignment="Right">
                    <TextBlock x:Name="MonthYearTextBlock" Text="חודש יוני 2026" FontSize="26" FontWeight="Bold" Foreground="#FF1493"/>
                    <TextBlock Text="לוח אימונים חודשי וניהול הרשמות" FontSize="14" Foreground="#666"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center">
                    <Button x:Name="BtnBackToHome" Content="🏠 חזרה לבית" Margin="5" Padding="10,6" 
                            Background="White" Foreground="#FF1493" BorderBrush="#FF1493" BorderThickness="1.5"
                            FontWeight="Bold" Cursor="Hand" Click="BtnBackToHome_Click">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="6"/>
                            </Style>
                        </Button.Resources>
                    </Button>

                    <Button x:Name="PrevMonthButton" Content="◀ החודש הקודם" Margin="5" Padding="8,4"  Background="White" Foreground="#FF1493" BorderBrush="#FF1493" BorderThickness="1.5" FontWeight="Bold" Cursor="Hand" Click="PrevMonthButton_Click"/>
                    <Button x:Name="NextMonthButton" Content="החודש הבא ▶" Margin="5" Padding="8,4" Background="White" Foreground="#FF1493" BorderBrush="#FF1493" BorderThickness="1.5" FontWeight="Bold" Cursor="Hand" Click="NextMonthButton_Click"/>

                    <Button x:Name="AddWorkoutButton" Content="+" FontSize="24" FontWeight="Bold" 
                            Background="#FF1493" Foreground="White" Width="45" Height="45" Margin="15,0,0,0"
                            HorizontalAlignment="Left" VerticalAlignment="Center" Cursor="Hand" 
                            Click="AddWorkoutButton_Click" Visibility="Collapsed">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="22"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                </StackPanel>
            </Grid>

            <ListBox x:Name="DaysListBox" Grid.Row="1" Height="70" Margin="0,0,0,15" SelectionChanged="DaysListBox_SelectionChanged">
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="#FF1493" BorderThickness="1" CornerRadius="5" Padding="10,5" Margin="2" Width="60" Background="White">
                            <StackPanel HorizontalAlignment="Center">
                                <TextBlock Text="{Binding DayNumber}" FontSize="16" FontWeight="Bold" HorizontalAlignment="Center" Foreground="#FF1493"/>
                                <TextBlock Text="{Binding DayOfWeekHebrew}" FontSize="11" HorizontalAlignment="Center" Foreground="#555"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="1*"/>
                </Grid.ColumnDefinitions>

                <ListView x:Name="WorkoutsListView" Grid.Column="0" Margin="0,0,10,0" SelectionChanged="WorkoutsListView_SelectionChanged">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Border CornerRadius="8" Padding="10" Background="White" Margin="2" Width="300">
                                <StackPanel>
                                    <TextBlock Text="{Binding WorkoutName}" FontSize="16" FontWeight="Bold" Foreground="#FF1493"/>
                                    <TextBlock Text="{Binding TimeStr}" FontSize="14" FontWeight="SemiBold"/>
                                    <TextBlock Text="{Binding TrainerName, StringFormat='מאמן: \{0\}'}" FontSize="12" Foreground="#666"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

                <Border x:Name="DetailsPanel" Grid.Column="1" Background="White" CornerRadius="10" Padding="15" Visibility="Collapsed">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <StackPanel Grid.Row="0">
                            <TextBlock x:Name="DetailWorkoutName" FontSize="18" FontWeight="Bold" Foreground="#FF1493" Margin="0,0,0,10"/>
                            <TextBlock x:Name="DetailTrainer" FontSize="13" Margin="0,2"/>
                            <TextBlock x:Name="DetailDate" FontSize="13" Margin="0,2"/>
                            <TextBlock x:Name="DetailTime" FontSize="13" Margin="0,2"/>
                            <TextBlock x:Name="DetailStatus" FontSize="13" FontWeight="SemiBold" Margin="0,2,0,10"/>

                            <TextBlock Text="מתאמנים רשומים בשיעור:" FontSize="14" FontWeight="Bold" Margin="0,10,0,5"/>
                        </StackPanel>

                        <ListView x:Name="RegisteredTraineesListView" Grid.Row="1" Margin="0,0,0,10">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding First_name}" FontSize="13" Padding="5"/>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>

                        <StackPanel Grid.Row="2">
                            <Button x:Name="ActionButton" Content="הרשמה לאימון" Background="#FF1493" Foreground="White" Height="35" FontWeight="Bold" Margin="0,5" Click="ActionButton_Click"/>
                            <Button x:Name="DeleteWorkoutButton" Content="מחק אימון" Background="Red" Foreground="White" Height="35" FontWeight="Bold" Margin="0,5" Click="DeleteWorkoutButton_Click" Visibility="Collapsed"/>
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>
        </Grid>

        <Popup x:Name="AddWorkoutPopup" Placement="Center" Width="360" Height="420" StaysOpen="False">
            <Border Background="White" CornerRadius="12" Padding="20" BorderBrush="#FF1493" BorderThickness="2">
                <StackPanel>
                    <Grid Margin="0,0,0,15">
                        <TextBlock Text="הוספת אימון חדש" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center" Foreground="#FF1493"/>
                        <Button Content="✕" Background="Transparent" BorderThickness="0" Foreground="#999" FontSize="16" HorizontalAlignment="Left" Click="BtnCancelPopup_Click" Cursor="Hand"/>
                    </Grid>

                    <TextBlock Text="תאריך אימון:" Margin="0,5,0,2"/>
                    <DatePicker x:Name="DpDate" Height="30"/>

                    <TextBlock Text="שעה (לדוגמה 15:30):" Margin="0,8,0,2"/>
                    <TextBox x:Name="TxtTime" Height="30" Padding="5,2"/>

                    <TextBlock Text="בחר סוג אימון:" Margin="0,8,0,2"/>
                    <ComboBox x:Name="CmbWorkoutKind" Height="30" Padding="5,2" DisplayMemberPath="Name_of_workout"/>

                    <TextBlock Text="קוד מאמן (ID):" Margin="0,8,0,2"/>
                    <TextBox x:Name="TxtTrainerId" Height="30" Padding="5,2" Background="#EAEAEA" IsReadOnly="True" Focusable="False"/>

                    <Grid Margin="0,20,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Button Grid.Column="0" Content="שמור אימון" Click="BtnSaveWorkout_Click" Background="#FF1493" Foreground="White" FontWeight="Bold" Height="35" Margin="5" Cursor="Hand"/>
                        <Button Grid.Column="1" Content="ביטול" Click="BtnCancelPopup_Click" Background="#666" Foreground="White" FontWeight="Bold" Height="35" Margin="5" Cursor="Hand"/>
                    </Grid>
                </StackPanel>
            </Border>
        </Popup>

        <Grid x:Name="LoadingOverlay" Background="#80FFFFFF" Visibility="Visible">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="80" Height="10" Foreground="#FF1493" Background="LightGray"/>
                <TextBlock Text="טוען נתונים מהשרת, אנא המתן..." FontSize="16" FontWeight="Bold" Foreground="#FF1493" Margin="0,10,0,0" HorizontalAlignment="Center"/>
            </StackPanel>
        </Grid>
    </Grid>
</Page>